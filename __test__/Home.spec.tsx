import * as React from 'react';
// import { render, screen, fireEvent } from '@testing-library/react-native';
import { HomeScreen } from '../components/screens/HomeScreen';

//TODO: yarn test throw jest config errors will debug them.
describe('Home screen display', () => {

  test.todo('Screen renders correctly')

  test.todo('check if the speech-to-text components is renders on the home screen')

  test.todo('Check if the generate file component renders on the screen when transcript is available ')
})
